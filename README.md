# Python-file-organizer
A small, robust Python script to automatically organize files in a directory into folders by file type (images, documents, archives, scripts, etc.). Supports dry-run and collision-safe moves.
